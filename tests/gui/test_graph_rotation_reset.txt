# Test: Graph rotation reset on selection change
# Expected: Graph should maintain rotation when selection changes

load_rom examples/lf9veb.bin
open_table "Load Scaling"
wait 300

# Open graph
open_graph
wait 500

# Screenshot initial position
screenshot graph_01_initial graph

# Select a cell to trigger selection change
select 2 2
wait 200
screenshot graph_02_after_first_select graph

# Now we need to simulate rotation - but test runner can't do that
# So let's check the code behavior instead
